// Human-authored English translations. Code is shared unchanged with the Korean
// source. Missing text/table translations throw at build time instead of falling back.
const table = (headers, rows) => ({ headers, rows });
export const english = {
  quickstart: { title:'Quick start', summary:'Write a queue, attach constraints, and pass it to your program. Tessembly describes supply; it is not an execution engine.', sections:{
    first:['Your first pattern', [null,'Choose four distinct pieces from the seven standard kinds. T and S must each precede I, without ordering T against S. I may be absent if both earlier kinds occur.','In RFC3, A<B means A first and A>B means B first. These are precedence symbols, not numeric positions. RFC1 files are never silently reinterpreted.']],
    build:['Build the native tools', ['Use Rust 1.85.0 and the committed Cargo.lock. Python is not required. Node.js and SvelteKit are needed only to develop the documentation; the optional npm package bundles its own Wasm asset.',null,'Core, text, relations and codec are separate libraries. Adopt document only when advanced declarations are needed. Conformance executables are not product runtime dependencies.']],
    compact:['Check a compact file',[null,'Compact fragments do not carry their semantic version, so specify the input profile. check validates syntax and provable local contradictions. NOT_CHECKED does not prove a satisfying queue or a PC solution.']],
    advanced:['Move to an advanced document',[null,null,'Advanced documents retain a version, immutable environment, structured supply and constraints. The final I in draw also requires I to occur in the scope.']]
  }},
  compact:{title:'Compact syntax',summary:'Add uppercase D and U constraints to Clearra-style queues and patterns. The input structure determines the exact scope.',sections:{
    patterns:['Supply expressions',[table(['Expression','Exact meaning'],[['IOTSZJL','Fixed supply order'],['P4','All orders of four distinct kinds selected from seven'],['P7P4','Concatenate two independent permutation segments'],['[ITO]','Choose one of I, T, O'],['[ITO]2','Choose two distinct kinds, in every order'],['[ITO]!','Permute the complete set'],['[^T]','Choose one standard kind other than T'],['* / *!','One arbitrary kind / all seven permutations'],['IOT;ITO','Union of two alternatives']]),'Repeated letters in brackets repeat a kind, not its count. [TTI]! is not a two-T bag. Use advanced bag or shuffle for multiplicities. Alternatives must have equal length in this profile.']],
    local:['Colons and braces',[null,'A colon attaches to the immediately preceding complete expression. Braces group a scope; brackets retain their choice meaning. D and U follow the same colon. Each block occurs at most once in a scope.','Braces do not create a bag. P3P3 differs from P6. Constraint scope, pattern segment and physical bag boundary are distinct.']],
    direction:['Comparators express precedence',[table(['RFC3','Meaning'],[['A<B','A precedes B'],['A>B','B precedes A'],['I>TS','T and S each precede I; no order between T/S'],['I>T<S','T precedes both I and S'],['T<IS','Equivalent to I>T<S']]),'Compare the first occurrence of each kind. Between groups, require every cross-pair comparison (AND). Overlapping groups such as I<IT contain a strict self-comparison and cannot be satisfied.',null]],
    presence:['Presence and absence',['A bare T requires T to occur. TS requires both kinds, not adjacency or OR. TT does not require two T pieces.',null,table(['Normalized earlier A / later B','Before(A,B)'],[['Both occur','True when first A precedes first B'],['Only A occurs','True'],['Only B occurs','False'],['Neither occurs','False']]),'T is the earlier kind in RFC3 D(I>T). T alone passes; I alone fails. This does not guarantee that I will eventually appear outside the scope. Do not confuse an unrevealed piece with absence from a completed scope. HAS(T), D? and U? are not RFC3 syntax.']],
    chain:['Commas and mixed chains',[null,'Commas mean AND. Expand a chain into adjacent comparisons only; do not invent an endpoint relation. Cycles are valid syntax. The checker reports UNSAT only for a contradiction it proves in the same scope and order domain.','D describes supply order. U describes actual use order of the source tokens in its scope. Do not combine D(I<T) and U(T<I) into one contradiction graph. A matching U relation does not establish legal hold operations or physical placement.']],
    counts:['Small reference cases',[table(['Pure pattern enumeration','Distinct queues'],[['P4','840'],['P4:D(T)','480'],['P4:D(TS)','240'],['P4:D(I>T)','360'],['P4:D(I>T,I)','120'],['P4:D(I>TS)','176'],['P4:D(I>T<S)','272']]),'These are set sizes without additional initial bag/hold filters. They are not probabilities or PC solution counts. External conformance tools compare the exact sets, not only the counts.']]
  }},
  advanced:{title:'Advanced declarations',summary:'Readable function-shaped data describes supply, environment, custom kinds and external references in one document.',sections:{
    structure:['Document structure',[null,'Declare config, supply, draw and use at most once each. Duplicate keys or named arguments are rejected even if their values agree. Declaration order never overrides the environment. reference and select may repeat with different identifiers.','Statements end with semicolons. The grammar supports // comments, UTF-8 strings, booleans, unsigned integers, lists, positional and named arguments. A positional argument cannot follow a named argument. There are no arbitrary scripts, loops, function execution or network calls.']],
    sources:['Supply functions',[table(['Function','Declared data'],[['pattern("P4") or "P4"','Compact supply AST'],['queue("IOT")','Exact finite queue'],['shuffle("TTI")','Distinct multiset permutations'],['bag("IOTSZJL")','Refilling bag stream'],['pool("IT")','Sampling-with-replacement stream'],['take(10, bag("IOTSZJL"))','Finite prefix of ten draws'],['repeat(3, shuffle("SZ"))','Three independent copies of a finite expression'],['concat(queue("IT"), shuffle("SZ"))','Concatenation'],['either(queue("IT"), queue("TI"))','Equal-length alternatives'],['external("vendor.rule", revision="v1", length=10)','Versioned external policy reference']]),'take cannot request more than a finite source contains. repeat requires a finite source. Concatenating anything after an unbounded stream is rejected. Parsing validates these length contracts without expanding permutations or drawing pieces.','Compact strings refer to standard kinds. Use lists for multi-character custom IDs. A source that cannot project to the standard relation tools remains SOURCE_REQUIRES_HOST, rather than being replaced with a guessed standard queue.']],
    relations:['Relation functions and custom kinds',[null,'before(A,B) states the normalized relation A first. present(A) requires presence. Standard kinds can also use draw(I>TS, I); the formatter expands it to before/present. For local constraints, use {}:D(...) inside a compact pattern in supply.','registry extends the standard I/O/T/S/Z/J/L IDs. Undeclared or duplicate IDs, wrong weight counts, zero weights and out-of-range weights are rejected. This is a custom multiset bag, not standard 7-bag.']],
    environment:['A single environment authority',['A fragment with missing settings can be read and stored. Before execution, resolve rule, start, see and hold once with the host. with_host_config fills missing keys only; conflicting values yield CONFIG_CONFLICT. There is no last-value-wins behavior.','The uniform policy in rule=seven_bag() is fixed. from_source means the supply declaration describes its policy. Declare custom host settings through external("id",revision="..."). The identifier never triggers an automatic code download.']],
    references:['References and QB/OQB decision points',[null,'A reference value is consumer-owned content or an identifier. format and value are required; page is zero-based. Tessembly does not verify that the external resource exists or inspect CTK3 cells.','select at=0 means the start decision; a positive value means after that many locks and the subsequent spawn. Values must increase; choices must resolve to declared references. This does not allow undoing earlier actions. The consumer validates observable common progress and feasible branches.']]
  }},
  state:{title:'Bags, observation and hold',summary:'Keep actual supply, available information and currently usable pieces as different state dimensions.',sections:{
    bag:['Standard bags and custom sources',['seven_bag() declares a uniform bag containing each standard kind once. With r kinds remaining, each has base next-draw probability 1/r. Initial leftovers can be declared separately as remainder("SZ", epoch=2).',null,'Do not infer physical bag boundaries from braces or pattern segments. Fixed queues and P3P3 are not silently edited to fit a standard bag. A host can validate actual bag legality against rule; validating the document alone does not prove game supply legality.','For multiplicities or weights, declare a custom bag/pool. Each positive integer weight corresponds to an individual listed token. bag draws from remaining tokens proportionally to their weights; pool draws with replacement. Omitted weights are one per token. Sampling and conditional probabilities belong to the consumer.']],
    view:['see-n is an explicit information contract',[null,table(['Field','Meaning / document-v1 default'],[['active','Reveal current piece / true'],['next','Directly revealed next-piece count / 5'],['hold','Reveal hold content / true'],['memory','history or current / history'],['reveal','supply, lock or host / supply'],['bag','known or hidden bag knowledge / known']]),'supply advances observation when actual supply is consumed; lock updates it at post-lock decision boundaries; host uses consumer-defined reveal events. all() exposes actual supply within the declared horizon. An ambiguous see-7 label alone is not stored.','An empty hold does not grant an extra hidden preview. Inferring the two possibilities SZ/ZS from bag rules does not enlarge the directly visible window. Unrevealed pieces are not absent pieces.']],
    hold:['Independent hold-state dimensions',[null,table(['Expression','State'],[['hold=none()','No hold feature or slot'],['hold=slot(initial=empty)','An available empty slot'],['initial=token("T",origin=9)','A held T with explicit origin'],['used=true','Hold already used this turn; re-hold locked'],['allowed=false','Default hold-action policy denies use'],['deny=["T"]','Deny hold while the pre-action active kind is T']]),'deny exists only in advanced declarations. It is not a D/U shortcut or a proposed Clearra UI feature. The held kind does not trigger it, and it does not forbid moving or placing T. Equal kinds may still have different origins.','Swapping occupied hold consumes no future queue entry. Empty hold consumes one actual next piece. Both lock re-hold for the turn; host-confirmed lock and spawn unlock it. A failed swap leaves state unchanged.']],
    adapter:['Missing information and unsupported states',['tail=pending means further supply information is needed; tail=end means supply has ended. Neither is a proof of zero solutions. Missing settings also do not become EMPTY or NONE implicitly.','If a dataset supports only occupied hold, EMPTY is UNSUPPORTED_STATE. Never invent a held piece for lookup. A consumer may provide a verified mapping preserving active/held, queue consumption, bag leftovers, revealed information and corresponding legal actions.','Dataset adapters and mappings are implemented by the user or other developers. A valid declaration does not establish dataset compatibility or PC success.']]
  }},
  wire:{title:'Binary formats and versions',summary:'Store the structure and environment without enumerating supply or reparsing source text.',sections:{
    formats:['Two explicit wire formats',[table(['Format','Purpose'],[['TSMB / AST wire v1','Standard-piece compact nodes, D/U conditions and source spans'],['TSDC / document wire v1','Environment, structured sources, custom IDs, relations and references']]),'TSMB is followed by wire=1 and RFC=3; TSDC by document-wire=1 and RFC=3. Binary versions and semantic profiles are independent of npm/package versions and test-port versions.','P7P4 is stored as two permutation nodes in a concatenation, not an expanded queue list. Compact storage does not prescribe a host execution representation. Never serialize raw C/Rust structure memory.']],
    layout:['Document-wire structure',['TSDC uses a 32-bit little-endian section count, IDs, critical flags and lengths. Section 1 is the critical document body. Values have distinct tags for booleans, u64, strings, identifiers, lists, call data and embedded TSMB AST. Strings are not executed or reparsed to recover semantics.','Standard pieces retain small TSMB codes and masks. Custom identifiers remain document registry/string IDs. Hold absence, emptiness, occupancy and turn lock are not collapsed into a single piece code.','Document wire v1 is deliberately simple structural encoding. It is not an optimal bit compressor and does not promise shortest encoding or zero-copy execution.']],
    safety:['Loss prevention and invalid input',['Unknown optional sections are preserved as opaque bytes. Unknown required sections yield UNSUPPORTED_CRITICAL_EXTENSION. Duplicate sections, truncated lengths, invalid tags, excessive nesting and trailing bytes are rejected.','With opaque metadata, to_text/doc-decode returns OPAQUE_METADATA_WOULD_BE_LOST rather than discarding it. Consumers must explicitly choose a preservation strategy. Structural roundtrips, including spans, are different from minimizing every logically equivalent document.']],
    limits:['Reference implementation limits',[table(['Item','Limit'],[['Text input','65,536 bytes'],['Nesting depth','48'],['Syntax nodes / predicates','4,096 each'],['Finite supply length','256'],['Advanced binary','1 MiB'],['Optional extensions','64']]),'These bound one format-processing request, not the entire host game. Hitting a limit is not successful empty supply or complete search. Large execution belongs to the external consumer.']]
  }},
  integrators:{title:'Integration guide',summary:'Adopt only the capabilities you need and test the real request path. Consumers connect datasets and game engines.',sections:{
    boundary:['Choose your components',[table(['Component','Responsibility'],[['tessembly-core','AST, errors and standard hold-supply state'],['tessembly-text','Compact parse/format'],['tessembly-relations','First occurrence, presence and local cycles'],['tessembly-codec','Standard pattern AST binary'],['tessembly-document','Advanced declarations, environment, references and document binary'],['tessembly-cli','File commands and reference test ports'],['tessembly-conformance','Independent tools for external consumers']]),'Library consumers need not distribute the CLI, documentation site or conformance executables. The TCK does not import the product parser/evaluator to manufacture expected answers; it uses small independent oracles and fixed vectors.']],
    tck:['Connect a test port to your application',[null,'The compact port is tessembly.test-port.v1. Requests include id, protocol, profile and op. Operations cover capabilities, compile, enumerate_D, evaluate_U_witness, encode/decode, hold_step, check_adapter and resolve_config. stdout is JSON Lines only; send diagnostics to stderr.',null,'Advanced documents use tessembly.document-test-port.v1 and the reference CLI doc-port: validate, roundtrip, project, decode, hold and resolve. project only projects a standard AST; it does not execute the environment. Carry the returned environment along with the projection.','The runner starts a trusted developer command. It is not a sandbox. A passing report is evidence for its exact profile and finite cases, not automatic certification of a host or dataset.']],
    e2e:['Verify the real connection',['Wire the test port through actual input, request compilation, execution boundary and output. Passing a separate test-only parser does not test the application. External developers provide GUI widget adapters for E2E tests.','Check that changing D/U, scope or NONE/EMPTY/occupied hold changes real output as required. Changing only hidden future supply must not affect a choice made from identical allowed observation histories. Branching after a legal reveal remains allowed.']],
    results:['Result contracts',[table(['Status','Meaning'],[['INVALID_SYNTAX / INVALID_DOCUMENT','Syntax or structure violation'],['CONFIG_CONFLICT','Explicit environment conflict'],['UNSAT','Evidence of impossibility in the stated domain'],['NOT_CHECKED','Not sufficiently checked'],['UNSUPPORTED / UNSUPPORTED_STATE','Capability or state outside support'],['INCOMPLETE','Bounded work or cancellation prevented completion']]),'Matching relations, legal supply, hold transitions and successful placements are different claims. Distinguish complete=true with exactly zero results from incomplete work. A use-order check is not a PC solution.']],
    datasets:['External datasets and resources',['Users or external developers implement HF, Clearra, CTK3, Fumen and Sfinder integrations. Tessembly preserves their declarations and references; it never opens their URLs or queries their databases.','Consumers verify revisions, graph/state indices, piece IDs, hold assumptions, bag positions, observation contracts and game rules. A recommended action per state is not all solutions. Dropping unsupported constraints is not equivalent conversion.']]
  }},
  tools:{title:'Conformance tools',summary:'Optional tools for external developers to test their programs through standard ports. Not a product runtime dependency.',sections:{
    run:['Two executables, two contracts',['tessembly-tck checks compact syntax and standard supply/hold ports. tessembly-document-tck checks advanced documents, environment binding, custom declarations, binary roundtrips and advanced hold integration. Neither reads consumer source code.',null,'These commands test the provided reference CLI. For actual integration testing, replace the command after -- with your executable and arguments. Add .exe on Windows. Reports retain case names, outcomes, failing responses and semantic/transport versions.']],
    connect:['An advanced-port request',[null,'Send one JSON request per line and receive one response. Bind id and profile unchanged. validate checks syntax/declarations, not successful execution. DOCUMENT.md defines roundtrip, project, decode, hold and resolve.','The advanced port tests representation and boundaries. Database queries, information restrictions and physical placement need consumer-owned integration fixtures. A port that bypasses the app by calling a reference CLI does not validate the app.']],
    boundaries:['Zero results are not incomplete work',['Do not substitute another policy or hold state for unsupported input. Missing supply is NOT_CHECKED; bounded work is INCOMPLETE; a proved contradiction is UNSAT in its domain. Do not merge D/U or unrelated scopes.','The runner bounds output and runtime and checks malformed JSON, profiles and duplicate results. It runs a trusted command, not arbitrary code in a sandbox. Do not ship the test executables as library runtime dependencies.']]
  }},
  scope:{title:'Project boundaries',summary:'A small, independent format. External search engines and datasets do not become hidden product requirements.',sections:{
    owns:['What Tessembly provides',['Compact/advanced parsing, normalization, setting/reference validation, first-occurrence relations, local impossibility diagnostics, structural binary interchange, small hold-supply transitions and a CLI. The static SvelteKit site explains these contracts.','Custom pieces, bags, weights, external policies, see-n and QB/OQB decision points are validated and preserved as declarations. Expressing a policy differs from executing or optimizing it.']],
    host:['What the consumer provides',['PC search, geometry, rotations, reachability, gameplay, see-n evaluation, weighted sampling, stateful supply execution and replay reconstruction belong to external programs. They are not an internal unfinished-feature backlog.','Users or external developers implement Clearra/CTK3/Fumen/Sfinder adapters and HF or other dataset connections. This repository does not automatically query external resources or download and execute their code.']],
    tests:['Conformance tools are for external developers',['tessembly-conformance is an independent developer tool for a consumer application’s real I/O boundary. It is not a required runtime or search engine. Running it against the reference CLI in repository CI is a regression check of the shipped tools and reference implementation.','Finite test success is not a universal correctness proof or certification. Connect real application paths, not a test-only alternative parser. Consumer developers provide GUI E2E adapters.']],
    versions:['Versioned compatibility',['Semantic profile, document schema, binary wire and test-port protocol versions are separate. RFC1 is rejected rather than guessed or migrated. Consumers unable to execute a custom policy or resolve a reference must return UNSUPPORTED/UNSUPPORTED_STATE, not discard the condition.']]
  }}
};
/** @param {import('./types').Chapter} source @returns {import('./types').Chapter} */
export function translateChapter(source) {
  const tr=english[source.slug];
  if(!tr) throw new Error(`Missing English chapter: ${source.slug}`);
  return {...source,title:tr.title,summary:tr.summary,sections:source.sections.map(section=>{
    const translated=tr.sections[section.id];
    if(!translated || translated[1].length!==section.blocks.length) throw new Error(`English section mismatch: ${source.slug}/${section.id}`);
    return {...section,title:translated[0],blocks:section.blocks.map((block,i)=>{
      const text=translated[1][i];
      if(block.kind==='code') { if(text!==null) throw new Error('Code must be shared'); return block; }
      if(block.kind==='table') { if(!text || typeof text!=='object') throw new Error('Missing table translation'); return {...block,...text}; }
      if(typeof text!=='string') throw new Error('Missing prose translation');
      return {...block,text};
    })};
  })};
}
