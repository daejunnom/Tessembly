const p=text=>({kind:'p',text});
const code=(text,language='javascript')=>({kind:'code',text,language});
const install='npm install tessembly\n# Verified tarball / 검증된 배포 파일\nnpm install ./tessembly-0.1.1.tgz';
const sample="import { createTessembly } from 'tessembly';\nconst t = await createTessembly();\nconsole.log(t.checkPattern('P4:D(I<TS)'));\nconst bytes = t.encodePattern('P4:D(T)');\nconsole.log(t.decodePattern(bytes));\nt.dispose();";
/** @type {Record<string,import('./types').Chapter>} */
export const npmChapters={
 en:{slug:'npm',title:'npm and languages',kicker:'JAVASCRIPT / INTERNATIONALIZATION',summary:'The same Rust implementation, a bundled Wasm asset and no npm runtime dependencies. Korean for a Korean primary language; English otherwise.',sections:[
 {id:'use',title:'Use the library',blocks:[p('The package source is packages/npm. The initial 0.1.0 release is published. The next package version, 0.1.1, adds MIT metadata and the license text. A source version or passing build alone does not prove that its registry release is available.'),code(install,'shell'),code(sample),p('Node.js 22+ is required. Browser applications use tessembly/browser. Supply wasmUrl when a bundler needs an explicit copied asset, or supply wasm bytes/Module to avoid fetching. No Rust, Python, native addon or install script runs on the consumer machine.')]},
 {id:'dependencies',title:'Zero-dependency boundary',blocks:[p('All npm dependency categories are empty, including peer/optional/bundled and development dependencies. The separate Wasm build uses only Rust standard-library and local format crates. The compiled module has no imports and ships in the tarball.'),p('Node built-ins and browser WebAssembly/TextEncoder APIs are platform facilities. Svelte documentation and external conformance tools are separate. Native CLI/TCK JSON transport still uses serde_json, which is not linked into the npm Wasm build. The npm API handles parsing, checks and codecs, not a solver or dataset adapter.')]},
 {id:'language',title:'Language selection',blocks:[p('At neutral Pages URLs, the browser primary language selects ko only for Korean; all other languages select en. A secondary Korean preference does not override a non-Korean primary language. Explicit /en/ and /ko/ URLs win; manual selection is saved when browser storage is available. JavaScript-disabled visitors see English at neutral URLs and can follow language links.'),p('The npm library uses browser primary language or the desktop/Node locale. language and setLanguage override library presentation; TESSEMBLY_LANG and --lang override the CLI. Codes, byte spans, statuses, syntax and stored data remain language-independent.'),code("const t = await createTessembly({ language: 'ko' });\nt.setLanguage('en');"),p('Both documentation languages are authored and prerendered. No language model, translation service or runtime i18n package is used.')]},
 {id:'publish',title:'OIDC publishing',blocks:[p('Different npm and GitHub login emails are not a publishing blocker. The npm owner must configure Trusted Publisher for owner daejunnom, repository Tessembly, workflow filename npm-release.yml and environment npm. Enable direct npm publish; stage-only permission is not enough for this workflow.'),p('Start a new Publish npm package run on main, expected_version=0.1.1. mode=verify-only builds and checks but does NOT test npm authorization. mode=publish publishes with OIDC and verifies registry license and integrity. The workflow never loads NPM_TOKEN and does not automatically fall back to token authentication.'),p('After a real OIDC release succeeds, revoke the bootstrap npm token and delete any NPM_TOKEN repository/environment Secret. These are separate actions. Keep 2FA enabled. A skipped Trusted publish step in the historical token release was normal, not evidence of OIDC readiness. See docs/PUBLISHING.md in the repository for the complete setup.')]},
 {id:'license',title:'MIT license',blocks:[p('Tessembly project sources and documentation are licensed under MIT. Copyright (c) 2026 daejunnom. The npm package includes the same LICENSE text as the repository and declares license: MIT; third-party components retain their original licenses.'),p('The 0.1.0 registry tarball is not rewritten. The new MIT package is prepared as 0.1.1 and must pass the explicit release workflow before it is publicly available. The semantic profile and binary wire versions are unchanged.')]}]},
 ko:{slug:'npm',title:'npm과 다국어',kicker:'JAVASCRIPT / INTERNATIONALIZATION',summary:'동일한 Rust 구현과 포함된 Wasm을 추가 npm 의존성 없이 사용합니다. 주 언어가 한국어이면 한국어, 나머지는 영어입니다.',sections:[
 {id:'use',title:'라이브러리 사용',blocks:[p('패키지 소스는 packages/npm입니다. 최초 0.1.0은 공개 배포되었고, 다음 버전 0.1.1에는 MIT 메타데이터와 라이선스 본문을 추가했습니다. 소스의 버전이나 빌드 성공만으로 해당 버전이 레지스트리에 공개됐다고 판단하지 마세요.'),code(install,'shell'),code(sample),p('Node.js 22 이상이 필요합니다. 브라우저는 tessembly/browser를 사용하고 번들러에 따라 wasmUrl로 복사한 자산을 지정하거나 wasm 바이트/Module을 전달합니다. 소비자 환경에서 Rust/Python·네이티브 애드온·설치 스크립트를 실행하지 않습니다.')]},
 {id:'dependencies',title:'zero-dependency의 경계',blocks:[p('npm의 dependencies·peer·optional·bundled·devDependencies는 모두 비어 있습니다. 별도 Wasm 빌드는 Rust 표준 라이브러리와 내부 형식 크레이트만 사용합니다. 컴파일된 모듈은 import가 없고 패키지 압축 파일에 포함됩니다.'),p('Node 기본 모듈과 브라우저 WebAssembly/TextEncoder는 플랫폼 기능입니다. Svelte 문서와 외부 적합성 도구는 별도입니다. 네이티브 CLI/TCK의 JSON 전송에는 serde_json이 남지만 npm Wasm에는 연결되지 않습니다. npm API는 파싱·검사·코덱을 제공하며 탐색기·데이터셋 어댑터가 아닙니다.')]},
 {id:'language',title:'언어 선택',blocks:[p('언어 경로가 없는 Pages 주소는 브라우저 주 언어가 한국어일 때만 ko, 나머지는 en을 선택합니다. 한국어가 보조 언어 목록에 있어도 주 언어를 바꾸지 않습니다. /en/·/ko/ 주소는 자동 감지보다 우선하며 수동 선택은 저장소 사용이 가능할 때 보존합니다. JavaScript를 끄면 중립 주소에서 영어가 보이고 언어 링크로 이동할 수 있습니다.'),p('npm은 브라우저 주 언어 또는 데스크톱/Node 로캘을 사용합니다. 라이브러리는 language와 setLanguage, CLI는 TESSEMBLY_LANG과 --lang으로 표시 언어를 바꿉니다. 오류 코드·바이트 위치·기계 상태·문법·저장 데이터는 번역하지 않습니다.'),code("const t = await createTessembly({ language: 'ko' });\nt.setLanguage('en');"),p('문서는 두 언어로 직접 작성하고 정적 생성합니다. 언어 모델·번역 서비스·런타임 i18n 의존성은 사용하지 않습니다.')]},
 {id:'publish',title:'OIDC 배포',blocks:[p('npm과 GitHub의 로그인 이메일이 달라도 배포할 수 있습니다. npm 소유자가 Trusted Publisher에서 owner=daejunnom, repository=Tessembly, workflow filename=npm-release.yml, environment=npm을 등록해야 합니다. 직접 npm publish를 허용하세요. staged 배포만 허용한 설정으로는 이 워크플로를 사용할 수 없습니다.'),p('main에서 Publish npm package의 새 실행을 시작하고 expected_version=0.1.1을 지정합니다. mode=verify-only는 빌드·파일 검사이며 npm 인증을 검증하지 않습니다. mode=publish는 OIDC로 배포하고 registry 라이선스와 무결성을 확인합니다. NPM_TOKEN을 읽지 않고 토큰 인증으로 자동 대체하지 않습니다.'),p('실제 OIDC 배포가 성공한 뒤 최초 npm 토큰을 폐기하고 repository/environment의 NPM_TOKEN Secret도 삭제합니다. 이 둘은 별개 작업이며 계정 2FA는 유지합니다. 과거 token 배포에서 Trusted publish 단계가 생략된 것은 정상이나 OIDC 준비 완료의 증거는 아닙니다. 상세 절차는 저장소 docs/PUBLISHING.md에 있습니다.')]},
 {id:'license',title:'MIT 라이선스',blocks:[p('테섬블리 프로젝트의 소스와 문서는 MIT 라이선스입니다. Copyright (c) 2026 daejunnom. npm에는 저장소와 동일한 LICENSE 본문을 포함하고 license: MIT를 선언합니다. 제3자 구성요소는 원래 라이선스를 유지합니다.'),p('기존 0.1.0 레지스트리 압축 파일은 다시 쓰지 않습니다. MIT 패키지는 0.1.1로 준비했으며 명시적인 배포 워크플로가 성공해야 공개됩니다. 의미 프로필과 바이너리 버전은 변경하지 않습니다.')]}]}
};
