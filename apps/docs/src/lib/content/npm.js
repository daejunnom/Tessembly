const p=text=>({kind:'p',text});
const code=(text,language='javascript')=>({kind:'code',text,language});
const sample="import { createTessembly } from 'tessembly';\nconst t = await createTessembly();\nconsole.log(t.checkPattern('P4:D(I<TS)'));\nconst bytes = t.encodePattern('P4:D(T)');\nconsole.log(t.decodePattern(bytes));\nt.dispose();";
/** @type {Record<string,import('./types').Chapter>} */
export const npmChapters={
 en:{slug:'npm',title:'npm and languages',kicker:'JAVASCRIPT / INTERNATIONALIZATION',summary:'The same Rust implementation, a bundled Wasm asset and no npm runtime dependencies. Korean for a Korean primary language; English otherwise.',sections:[
 {id:'use',title:'Use the library',blocks:[p('The package source is packages/npm. npm package contracts produces a verified tarball. Initial publication still requires npm owner authentication; a passing build alone is not a public registry release.'),code('npm install ./tessembly-0.1.0.tgz','shell'),code(sample),p('After publication, npm install tessembly installs the same package. Node.js 22+ is required. Browser applications use tessembly/browser. Supply wasmUrl when a bundler needs an explicit copied asset, or supply wasm bytes/Module to avoid fetching. No Rust, Python, native addon or install script runs on the consumer machine.')]},
 {id:'dependencies',title:'Zero-dependency boundary',blocks:[p('All npm dependency categories are empty, including peer/optional/bundled and development dependencies. The separate Wasm build uses only Rust standard-library and local format crates. The compiled module has no imports and ships in the tarball.'),p('Node built-ins and browser WebAssembly/TextEncoder APIs are platform facilities. Svelte documentation and external conformance tools are separate. Native CLI/TCK JSON transport still uses serde_json, which is not linked into the npm Wasm build. The npm API handles parsing, checks and codecs, not a solver or dataset adapter.')]},
 {id:'language',title:'Language selection',blocks:[p('At neutral Pages URLs, the browser primary language selects ko only for Korean; all other languages select en. A secondary Korean preference does not override a non-Korean primary language. Explicit /en/ and /ko/ URLs win; manual selection is saved when browser storage is available. JavaScript-disabled visitors see English at neutral URLs and can follow language links.'),p('The npm library uses browser primary language or the desktop/Node locale. language and setLanguage override library presentation; TESSEMBLY_LANG and --lang override the CLI. Codes, byte spans, statuses, syntax and stored data remain language-independent.'),code("const t = await createTessembly({ language: 'ko' });\nt.setLanguage('en');"),p('Both documentation languages are authored and prerendered. No language model, translation service or runtime i18n package is used.')]},
 {id:'publish',title:'Owner authorization',blocks:[p('Different npm and GitHub login emails are not themselves a publishing blocker. The npm owner authorizes credentials or an exact repository/workflow. Linked Accounts & Recovery Option is account recovery, not publish authorization.'),p('First publish the verified tarball after local npm login, or add a short-lived granular publishing token as repository Secret NPM_TOKEN. Never paste it into chat or source. Noninteractive token publishing needs the appropriate write and, when applicable, explicit 2FA-bypass permission; do not disable account 2FA.'),p('After initial publication, configure Trusted Publishing for GitHub owner daejunnom, repository Tessembly, workflow npm-release.yml and environment npm. Permit npm publish for the direct workflow. Verify OIDC before revoking the bootstrap token. The owner has not selected an open-source license; package metadata remains UNLICENSED.')]}]},
 ko:{slug:'npm',title:'npm과 다국어',kicker:'JAVASCRIPT / INTERNATIONALIZATION',summary:'동일한 Rust 구현과 포함된 Wasm을 추가 npm 의존성 없이 사용합니다. 주 언어가 한국어이면 한국어, 나머지는 영어입니다.',sections:[
 {id:'use',title:'라이브러리 사용',blocks:[p('패키지 소스는 packages/npm입니다. npm package contracts가 검증된 압축 파일을 만듭니다. 최초 공개 배포에는 npm 소유자 인증이 필요하며 빌드 통과만으로 레지스트리 배포가 완료되는 것은 아닙니다.'),code('npm install ./tessembly-0.1.0.tgz','shell'),code(sample),p('공개 배포 뒤에는 npm install tessembly를 사용합니다. Node.js 22 이상이 필요합니다. 브라우저는 tessembly/browser를 사용하고 번들러에 따라 wasmUrl로 복사한 자산을 지정하거나 wasm 바이트/Module을 전달합니다. 소비자 환경에서 Rust/Python·네이티브 애드온·설치 스크립트를 실행하지 않습니다.')]},
 {id:'dependencies',title:'zero-dependency의 경계',blocks:[p('npm의 dependencies·peer·optional·bundled·devDependencies는 모두 비어 있습니다. 별도 Wasm 빌드는 Rust 표준 라이브러리와 내부 형식 크레이트만 사용합니다. 컴파일된 모듈은 import가 없고 패키지 압축 파일에 포함됩니다.'),p('Node 기본 모듈과 브라우저 WebAssembly/TextEncoder는 플랫폼 기능입니다. Svelte 문서와 외부 적합성 도구는 별도입니다. 네이티브 CLI/TCK의 JSON 전송에는 serde_json이 남지만 npm Wasm에는 연결되지 않습니다. npm API는 파싱·검사·코덱을 제공하며 탐색기·데이터셋 어댑터가 아닙니다.')]},
 {id:'language',title:'언어 선택',blocks:[p('언어 경로가 없는 Pages 주소는 브라우저 주 언어가 한국어일 때만 ko, 나머지는 en을 선택합니다. 한국어가 보조 언어 목록에 있어도 주 언어를 바꾸지 않습니다. /en/·/ko/ 주소는 자동 감지보다 우선하며 수동 선택은 저장소 사용이 가능할 때 보존합니다. JavaScript를 끄면 중립 주소에서 영어가 보이고 언어 링크로 이동할 수 있습니다.'),p('npm은 브라우저 주 언어 또는 데스크톱/Node 로캘을 사용합니다. 라이브러리는 language와 setLanguage, CLI는 TESSEMBLY_LANG과 --lang으로 표시 언어를 바꿉니다. 오류 코드·바이트 위치·기계 상태·문법·저장 데이터는 번역하지 않습니다.'),code("const t = await createTessembly({ language: 'ko' });\nt.setLanguage('en');"),p('문서는 두 언어로 직접 작성하고 정적 생성합니다. 언어 모델·번역 서비스·런타임 i18n 의존성은 사용하지 않습니다.')]},
 {id:'publish',title:'소유자의 배포 인증',blocks:[p('npm과 GitHub 로그인 이메일이 달라도 그 자체로 배포가 막히지는 않습니다. npm 소유자가 인증 수단이나 정확한 저장소·워크플로를 승인해야 합니다. Linked Accounts & Recovery Option은 계정 복구용이지 배포 권한이 아닙니다.'),p('최초 배포는 로컬 npm 로그인 뒤 검증된 압축 파일을 사용하거나, 짧은 유효기간의 granular 배포 토큰을 저장소 Secret NPM_TOKEN에 등록합니다. 토큰을 대화나 소스에 넣지 마세요. 비대화형 배포에는 적절한 쓰기 권한과 필요한 경우 명시적 2FA 우회 권한이 필요하며 계정 2FA 자체를 끄지 않습니다.'),p('최초 등록 뒤 Trusted Publishing에서 GitHub owner=daejunnom, repository=Tessembly, workflow=npm-release.yml, environment=npm을 지정합니다. 직접 배포 워크플로이므로 npm publish를 허용해야 합니다. OIDC 확인 뒤 최초 토큰을 폐기합니다. 소유자가 아직 오픈소스 라이선스를 선택하지 않아 메타데이터는 UNLICENSED로 남습니다.')]}]}
};
