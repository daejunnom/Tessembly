const p=text=>({kind:'p',text});
const code=(text,language='javascript')=>({kind:'code',text,language});
const install='npm install tessembly\n# Verified tarball / 검증된 배포 파일\nnpm install ./tessembly-0.1.2.tgz';
const sample="import { createTessembly } from 'tessembly';\nconst t = await createTessembly();\nconsole.log(t.checkPattern('P4:D(I<TS)'));\nconst bytes = t.encodePattern('P4:D(T)');\nconsole.log(t.decodePattern(bytes));\nt.dispose();";
/** @type {Record<string,import('./types').Chapter>} */
export const npmChapters={
 en:{slug:'npm',title:'npm and languages',kicker:'JAVASCRIPT / INTERNATIONALIZATION',summary:'The same Rust implementation, a bundled Wasm asset and no npm runtime dependencies. Korean for a Korean primary language; English otherwise.',sections:[
 {id:'use',title:'Use the library',blocks:[p('The package source is packages/npm. Version 0.1.1 is already published under MIT through OIDC. Version 0.1.2 is the platform and resource-hardening candidate. A passing build alone does not prove registry publication.'),code(install,'shell'),code(sample),p('Node.js 22+ is required. Browser applications use tessembly/browser. Supply wasmUrl when the bundler needs an explicitly copied asset, or trusted wasm bytes/Module to avoid fetching. No Rust, Python, native addon or installation script runs on the consumer machine.')]},
 {id:'dependencies',title:'Zero-dependency boundary',blocks:[p('All npm dependency categories are empty, including peer/optional/bundled and development dependencies. The Wasm build uses only the Rust standard library and local format crates. The compiled module has no imports and ships in the tarball.'),p('Node built-ins and browser WebAssembly/TextEncoder are platform facilities. Svelte documentation and external conformance tools remain separate. Native CLI/TCK JSON still uses serde_json, not linked into npm Wasm. The API handles formats, not solvers or datasets.')]},
 {id:'language',title:'Language selection',blocks:[p('Neutral Pages URLs select ko only for a Korean primary browser language. Every other primary language selects en, even when Korean is a secondary preference. Explicit /en/ and /ko/ URLs win; manual preferences are saved when storage is available. JavaScript-disabled visitors get readable English and language links.'),p('The npm library uses browser primary language or desktop/Node locale. language/setLanguage override presentation; TESSEMBLY_LANG and --lang override CLI preferences. Codes, byte spans, machine states, syntax and stored bytes remain language-independent.'),code("const t = await createTessembly({ language: 'ko' });\nt.setLanguage('en');"),p('Both documentation languages are authored and prerendered. No language model, translation service or runtime i18n dependency is used.')]},
 {id:'publish',title:'OIDC publishing and recovery',blocks:[p('Trusted Publisher remains GitHub owner daejunnom, repository Tessembly, filename npm-release.yml and environment npm, with direct npm publish permission. Different login emails are not a blocker. The working OIDC setup does not need recreating.'),p('The 0.1.1 upload succeeded; only its old final checker failed after about 38 seconds. The original registry artifact is now confirmed read-only. Do not delete or republish it. Start a NEW release for the hardening candidate: main / mode=publish / expected_version=0.1.2. verify-only does not test npm authorization.'),p('The workflow never loads NPM_TOKEN or silently falls back to token authentication. After an actual verified OIDC release, revoke the bootstrap npm token and delete its GitHub Secret separately. Keep 2FA enabled. See the security chapter and docs/PUBLISHING.md for exact-artifact recovery.')]},
 {id:'license',title:'MIT license',blocks:[p('Tessembly sources and documentation are MIT. Copyright (c) 2026 daejunnom. npm contains the same LICENSE as the repository; third-party components retain their own licenses.'),p('Published 0.1.0 and 0.1.1 tarballs are not rewritten. The 0.1.2 hardening candidate must pass the explicit release workflow before publication. Semantic profiles and binary wire versions are unchanged.')]}]},
 ko:{slug:'npm',title:'npm과 다국어',kicker:'JAVASCRIPT / INTERNATIONALIZATION',summary:'동일한 Rust 구현과 포함된 Wasm을 추가 npm 의존성 없이 사용합니다. 주 언어가 한국어이면 한국어, 나머지는 영어입니다.',sections:[
 {id:'use',title:'라이브러리 사용',blocks:[p('패키지 소스는 packages/npm입니다. 0.1.1은 OIDC를 통해 MIT로 이미 공개되었습니다. 0.1.2는 실행 환경·자원 제한 보강 후보입니다. 빌드 성공만으로 레지스트리 공개를 판단하지 마세요.'),code(install,'shell'),code(sample),p('Node.js 22 이상이 필요합니다. 브라우저는 tessembly/browser를 사용합니다. 번들러에 따라 wasmUrl로 복사한 자산을 지정하거나 신뢰한 wasm 바이트/Module을 전달합니다. 소비자 환경에서 Rust/Python·네이티브 애드온·설치 스크립트를 실행하지 않습니다.')]},
 {id:'dependencies',title:'zero-dependency의 경계',blocks:[p('npm의 dependencies·peer·optional·bundled·devDependencies는 모두 비어 있습니다. Wasm 빌드는 Rust 표준 라이브러리와 내부 형식 크레이트만 사용하며 컴파일한 모듈은 import 없이 압축 파일에 포함됩니다.'),p('Node 기본 모듈과 브라우저 WebAssembly/TextEncoder는 플랫폼 기능입니다. Svelte 문서와 외부 적합성 도구는 별도입니다. 네이티브 CLI/TCK의 serde_json은 npm Wasm에 연결되지 않습니다. API는 형식을 다루며 탐색·데이터셋 기능이 아닙니다.')]},
 {id:'language',title:'언어 선택',blocks:[p('중립 Pages 주소는 브라우저 주 언어가 한국어일 때만 ko를 선택합니다. 한국어가 보조 언어로 있어도 나머지 주 언어는 en입니다. /en/·/ko/ 주소가 우선하고 수동 선택은 저장소 사용이 가능할 때 보존됩니다. JavaScript를 꺼도 영어 본문과 언어 링크를 제공합니다.'),p('npm은 브라우저 주 언어 또는 데스크톱/Node 로캘을 사용합니다. language/setLanguage 및 CLI의 TESSEMBLY_LANG·--lang으로 변경합니다. 오류 코드·바이트 위치·기계 상태·문법·저장 데이터는 번역하지 않습니다.'),code("const t = await createTessembly({ language: 'ko' });\nt.setLanguage('en');"),p('두 언어 문서를 직접 작성하고 정적 생성합니다. 언어 모델·번역 서비스·런타임 i18n 의존성을 사용하지 않습니다.')]},
 {id:'publish',title:'OIDC 배포와 확인 복구',blocks:[p('Trusted Publisher의 owner=daejunnom, repository=Tessembly, filename=npm-release.yml, environment=npm과 직접 npm publish 권한을 유지합니다. 로그인 이메일이 달라도 문제가 아니며 성공한 OIDC 설정을 재등록할 필요가 없습니다.'),p('0.1.1 업로드는 성공했고 이전 최종 검사만 약 38초 뒤 실패했습니다. 원래 공개 파일은 읽기 전용으로 확인됐으니 삭제·재업로드하지 마세요. 보강 후보는 새로운 실행에서 main / mode=publish / expected_version=0.1.2로 배포합니다. verify-only는 npm 인증을 검증하지 않습니다.'),p('NPM_TOKEN을 읽거나 토큰 인증으로 자동 대체하지 않습니다. 실제 OIDC 공개 확인 후 최초 npm 토큰을 폐기하고 GitHub Secret도 별도로 삭제하세요. 계정 2FA는 유지하며 정확한 파일 복구 절차는 보안 문서와 docs/PUBLISHING.md에 있습니다.')]},
 {id:'license',title:'MIT 라이선스',blocks:[p('테섬블리 소스와 문서는 MIT입니다. Copyright (c) 2026 daejunnom. npm에도 저장소와 동일한 LICENSE를 포함하며 제3자 구성요소는 각자의 라이선스를 유지합니다.'),p('공개된 0.1.0·0.1.1 파일은 다시 쓰지 않습니다. 0.1.2 보강 후보는 명시적인 배포 워크플로가 성공해야 공개됩니다. 의미 프로필·바이너리 버전은 바꾸지 않습니다.')]}]}
};
