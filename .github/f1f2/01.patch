diff --git a/Cargo.lock b/Cargo.lock
index 820ceac..f9f3f18 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -93,7 +93,7 @@ dependencies = [
 
 [[package]]
 name = "tessembly-cli"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = [
  "serde_json",
  "tessembly-codec",
@@ -105,25 +105,25 @@ dependencies = [
 
 [[package]]
 name = "tessembly-codec"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = [
  "tessembly-core",
 ]
 
 [[package]]
 name = "tessembly-conformance"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = [
  "serde_json",
 ]
 
 [[package]]
 name = "tessembly-core"
-version = "0.2.0"
+version = "0.3.0"
 
 [[package]]
 name = "tessembly-document"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = [
  "tessembly-codec",
  "tessembly-core",
@@ -132,14 +132,14 @@ dependencies = [
 
 [[package]]
 name = "tessembly-relations"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = [
  "tessembly-core",
 ]
 
 [[package]]
 name = "tessembly-text"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = [
  "tessembly-core",
 ]
diff --git a/Cargo.toml b/Cargo.toml
index 4c08430..5d76570 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -3,7 +3,7 @@ members = ["crates/*"]
 resolver = "2"
 
 [workspace.package]
-version = "0.2.0"
+version = "0.3.0"
 edition = "2021"
 rust-version = "1.85"
 repository = "https://github.com/daejunnom/Tessembly"
diff --git a/bindings/wasm/Cargo.lock b/bindings/wasm/Cargo.lock
index c2f3290..7c8b863 100644
--- a/bindings/wasm/Cargo.lock
+++ b/bindings/wasm/Cargo.lock
@@ -4,29 +4,29 @@ version = 4
 
 [[package]]
 name = "tessembly-codec"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = ["tessembly-core"]
 
 [[package]]
 name = "tessembly-core"
-version = "0.2.0"
+version = "0.3.0"
 
 [[package]]
 name = "tessembly-document"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = ["tessembly-codec", "tessembly-core", "tessembly-text"]
 
 [[package]]
 name = "tessembly-relations"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = ["tessembly-core"]
 
 [[package]]
 name = "tessembly-text"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = ["tessembly-core"]
 
 [[package]]
 name = "tessembly-wasm"
-version = "0.2.0"
+version = "0.3.0"
 dependencies = ["tessembly-codec", "tessembly-core", "tessembly-document", "tessembly-relations", "tessembly-text"]
diff --git a/bindings/wasm/Cargo.toml b/bindings/wasm/Cargo.toml
index 206b2ad..a02e3f8 100644
--- a/bindings/wasm/Cargo.toml
+++ b/bindings/wasm/Cargo.toml
@@ -1,6 +1,6 @@
 [package]
 name = "tessembly-wasm"
-version = "0.2.0"
+version = "0.3.0"
 edition = "2021"
 rust-version = "1.85"
 license = "MIT"
diff --git a/bindings/wasm/src/lib.rs b/bindings/wasm/src/lib.rs
index 38661ff..dd64dec 100644
--- a/bindings/wasm/src/lib.rs
+++ b/bindings/wasm/src/lib.rs
@@ -80,7 +80,7 @@ fn dispatch(op: u32, input: &[u8]) -> Result<Vec<u8>> {
 // Rust pointer dereferences. The library has no unsafe blocks.
 #[no_mangle]
 pub extern "C" fn ts_abi_version() -> u32 {
-    2
+    3
 }
 #[no_mangle]
 pub extern "C" fn ts_reserve_input(len: u32) -> u32 {
diff --git a/crates/tessembly-cli/src/expand.rs b/crates/tessembly-cli/src/expand.rs
index 6947a5c..5822b50 100644
--- a/crates/tessembly-cli/src/expand.rs
+++ b/crates/tessembly-cli/src/expand.rs
@@ -1,7 +1,7 @@
 //! Small, budgeted developer oracle. Not the format parser or a PC search engine.
 use std::collections::BTreeSet;
 use tessembly_core::{Error, Node, NodeKind, Piece, Predicate, Result};
-use tessembly_relations::matches;
+use tessembly_relations::matches_checked;
 
 #[derive(Clone)]
 pub struct UseScope {
@@ -19,6 +19,10 @@ pub struct Budget {
     pub remaining: usize,
 }
 impl Budget {
+    fn charge(&mut self,n:usize)->Result<()> {
+        if n>self.remaining {return Err(Error::new("INCOMPLETE"));}
+        self.remaining-=n;self.steps+=n;Ok(())
+    }
     fn tick(&mut self) -> Result<()> {
         if self.remaining == 0 {
             return Err(Error::new("INCOMPLETE"));
@@ -36,7 +40,7 @@ fn push(out: &mut Vec<Variant>, v: Variant, budget: &mut Budget, cells: &mut usi
         v.queue.len()
             + v.uses
                 .iter()
-                .map(|s| s.predicates.len() * 8 + 3)
+                .map(|s| s.predicates.iter().map(Predicate::units).sum::<usize>() * 8 + 3)
                 .sum::<usize>(),
     );
     if out.len() >= MAX_VARIANTS || *cells > MAX_CELLS {
@@ -130,7 +134,13 @@ pub fn variants(root: &Node, budget: &mut Budget) -> Result<Vec<Variant>> {
             }
         }
         if let Some(ps) = &n.constraints.draw {
-            out.retain(|v| matches(&v.queue, ps));
+            let mut accepted=Vec::new();
+            let weight=ps.iter().map(Predicate::units).sum::<usize>();
+            for v in out {
+                b.charge(weight.saturating_mul(v.queue.len()+1))?;
+                if matches_checked(&v.queue,ps)? {accepted.push(v);}
+            }
+            out=accepted;
         }
         if let Some(ps) = &n.constraints.use_order {
             let existing: usize = out
@@ -139,13 +149,13 @@ pub fn variants(root: &Node, budget: &mut Budget) -> Result<Vec<Variant>> {
                     v.queue.len()
                         + v.uses
                             .iter()
-                            .map(|s| s.predicates.len() * 8 + 3)
+                            .map(|s| s.predicates.iter().map(Predicate::units).sum::<usize>() * 8 + 3)
                             .sum::<usize>()
                 })
                 .sum();
             let added = out
                 .len()
-                .checked_mul(ps.len().saturating_mul(8).saturating_add(3))
+                .checked_mul(ps.iter().map(Predicate::units).sum::<usize>().saturating_mul(8).saturating_add(3))
                 .ok_or_else(|| Error::new("INCOMPLETE"))?;
             if added > MAX_CELLS.saturating_sub(existing) {
                 return Err(Error::new("INCOMPLETE"));
@@ -165,7 +175,7 @@ pub fn variants(root: &Node, budget: &mut Budget) -> Result<Vec<Variant>> {
                 v.queue.len()
                     + v.uses
                         .iter()
-                        .map(|s| s.predicates.len() * 8 + 3)
+                        .map(|s| s.predicates.iter().map(Predicate::units).sum::<usize>() * 8 + 3)
                         .sum::<usize>()
             })
             .sum();
@@ -209,14 +219,13 @@ pub fn check_use(
             continue;
         }
         member = true;
-        if v.uses.iter().all(|scope| {
-            let projected: Vec<_> = order
-                .iter()
-                .filter(|i| **i >= scope.start && **i < scope.end)
-                .map(|i| queue[*i])
-                .collect();
-            matches(&projected, &scope.predicates)
-        }) {
+        let mut satisfied=true;
+        for scope in &v.uses {
+            let projected:Vec<_>=order.iter().filter(|i|**i>=scope.start && **i<scope.end).map(|i|queue[*i]).collect();
+            budget.charge(scope.predicates.iter().map(Predicate::units).sum::<usize>().saturating_mul(projected.len()+1))?;
+            if !matches_checked(&projected,&scope.predicates)? {satisfied=false;break;}
+        }
+        if satisfied {
             return Ok(Some(true));
         }
     }
diff --git a/crates/tessembly-cli/src/port.rs b/crates/tessembly-cli/src/port.rs
index e5f4b8d..862bc34 100644
--- a/crates/tessembly-cli/src/port.rs
+++ b/crates/tessembly-cli/src/port.rs
@@ -188,6 +188,7 @@ fn handle(req: &Value) -> Result<Value> {
         "capabilities" => {
             return Ok(json!({"status":"OK","complete":true,
             "operations":["compile","format","encode","decode","enumerate_D","evaluate_U_witness","hold_step","check_adapter","resolve_config"],
+            "filter_capabilities":["filters.logic.v1","filters.count.v1","filters.window.v1","filters.occurrence.v1"],
             "wire":"tessembly.ast-wire.v1.experimental","enumeration":"bounded-development-only",
             "limits":{"input_bytes":tessembly_core::MAX_INPUT,"max_draws":tessembly_core::MAX_DRAWS,"max_work":1000000},
             "not_implemented":["rfc1-migration","config-text","full-game-legality","see-n-policy-evaluation","custom-piece-codec","clearra-adapter","hf-lookup","gui-e2e","replay"]}))
@@ -278,7 +279,7 @@ fn handle(req: &Value) -> Result<Value> {
         Ok(n) => n,
         Err(e) => {
             return Ok(
-                json!({"status":if e.code.ends_with("LIMIT") {"INCOMPLETE"} else {"INVALID_SYNTAX"},
+                json!({"status":if e.code.ends_with("LIMIT") {"INCOMPLETE"} else if e.code.starts_with("UNSUPPORTED") {"UNSUPPORTED"} else {"INVALID_SYNTAX"},
             "complete":false,"error":{"code":e.code,"start":e.span.start,"end":e.span.end}}),
             )
         }
diff --git a/crates/tessembly-cli/tests/profile_migration.rs b/crates/tessembly-cli/tests/profile_migration.rs
index bc9fb38..f84df5d 100644
--- a/crates/tessembly-cli/tests/profile_migration.rs
+++ b/crates/tessembly-cli/tests/profile_migration.rs
@@ -98,8 +98,8 @@ fn unrecognized_optional_metadata_is_not_silently_relabelled() {
     );
 }
 #[test]
-fn planning_only_filters_and_observation_extensions_are_not_implemented() {
-    for s in ["P4:D(I<T||S)", "P4:D(!T)", "P4:D(T=2)", "P4:D(1-3:T)"] {
+fn unapproved_observation_extensions_and_noncanonical_filters_are_not_implemented() {
+    for s in ["P4:D(I<T||S)", "P4:D(T&&I)", "P4:D(T==2)", "P4:D(1-3:T)"] {
         assert!(tessembly_text::parse(s, PROFILE).is_err());
     }
     let d = format!("tessembly \"{PROFILE}\"; supply(\"P7\"); qb(\"setup\");");
